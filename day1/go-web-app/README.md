

###### ====================================================

% แก้lint fail


 % docker run --rm \        
  -v "$(pwd)":/app \
  -w /app \
  golang:1.22-alpine \
  gofmt -w .
###### ====================================================

| โจทย์ไฟล์ / สิ่งที่ใช้                  | รายละเอียด                                                                                                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **Source Code**                         | `main.go` เป็นไฟล์หลักที่เก็บโค้ดทั้งหมดของแอปพลิเคชัน                                                                       |
| **Package Manager**                     | ใช้ **Go Modules** สำหรับจัดการ dependency และ build โปรเจกต์ โดยสามารถติดตั้ง dependency ด้วยคำสั่ง `go mod download`       |
| **Dependency File**                     | `go.mod` เป็นไฟล์ที่ระบุชื่อ module และเวอร์ชันของ Go ที่ใช้ในโปรเจกต์ โดยโปรเจกต์นี้ไม่ได้ใช้ external dependency เพิ่มเติม |
| **Application Runtime**                 | ใช้ **Go Runtime** ซึ่งรองรับการทำงานแบบ concurrent ด้วย **goroutine-based event loop**                                      |
| **Application Package / OS Executable** | เมื่อ build แล้วจะได้เป็น **static binary** ที่สามารถนำไปรันบนระบบปฏิบัติการได้โดยตรงโดยไม่ต้องติดตั้ง runtime เพิ่มเติม     |
