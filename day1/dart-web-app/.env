# Infra config — อ่านตอน startup เท่านั้น
HOST=0.0.0.0
PORT=8080
# RUST_LOG=info

# App config — hot-reload ได้ แก้แล้วเห็นทันทีเมื่อ GET /
DATABASE_URI=postgres://user:pass@10.0.0.10:5432/mydb
REDIS_ENDPOINT=redis://10.0.0.2:6379
